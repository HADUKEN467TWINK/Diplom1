'' НАСТРОЙКИ: http://localhost:8000/docs - забиваем в браузер для открытия Swagger и взаимодействием API(Ручная загрузка zip-файла) http://localhost:8084

http://localhost:8001/docs - забиваем в браузер для открытия Swagger и взаимодействием API(Обновление через ссылку) http://localhost:8082

==localhost:8082(8084)== - база данных Mongo Express с веб-интерфейсом для просмотра содержимых данных ==localhost:8083== - Redis Commander Пользователь: admin Пароль: 123

Для ручной загрузки: Для ручной загрузки необходимо закинуть в папку "./Manual_download/download_zipfile (с двумя нижними подчеркиваниями в начале и конце названия)" необходимый архив и указать его имя в конфигурации, после чего можно вручную установить или обновить файл '''

Upd 1: Добавил контейнер. Следующий шаг: создание тестового репозитория с CVE файлами допустим за 2025 год и улучшение корректного сохранения в NoSQL бд.

Upd 2: Создал открытый тестовый репозиторий. Добавил Mongo Express в контейнер. Теперь можно клонировать официальный репозиторий с CVE в mongodb. Всё будет сохранятся корректно (сохраняет только json-файлы с CVE). Дальше буду делать пулл в существующую базу данных, а после авто-пулл.

Upd 3: Теперь добавление (не обновление!) базы данных делается через ссылку на ZIP-файл. Никаких гитов и репозиториев. Сделал соответствующую валидацию принимаемой на вход ссылки с применением pydantic. Добавить можно только ссылку с MIME-типами, которые показывают, что ссылка направлена на zip. Код скачивает zip-файл во временную директорию, в ней же разархивирует его, закидывает в монгу и стирает из директории. Добавлены обработчики событий. Протестировано с официальной зипкой.

{
  "status": true,
  "message": "Файлы успешно добавлены!",
  "statistics": {
    "Всего файлов": 314943,
    "Успешных обработок": 314943,
    "Ошибок": 0
  }
}

Upd 4: сделана ручка обновления, но работает некорректно. Изменена ручка скачивания базы и в целом есть небольшие изменения по всему коду. Ведутся тестирование с тестовым репозиториям. В финише будет проведено обновление базы данных через ручку с официальным файлом.

Upd 4.1: устранил наиглупейшую ошибку, на поиск которой ушло несколько часов. Всё работает корректно.

Upd 5: разделил проект и вынес все хранящиеся данные в папку database. Настроил импорт из этой папки через симлинки, но как я понял, после транспортировки куда-либо они преобразуются в обычные .py файлы. В любом случае хоть и колхоз вышел, но рабочий. В будущем настрою импорты через Path для надежности. Сделал установщик. Работает без интернета, разве что необходимые для работы образы нужно подгрузить заранее. Обновлен раздел НАСТРОЙКИ.

Upd 6: Завершены все установщики. Устранены самые очевидные уязвимости и написаны минимальные обработчики ошибок. Далее планирую вынести в отдельный контейнер настройки конфигурации, и сделать так, чтобы можно было менять только одно значение, а не весь словарь. Также нужно заблокировать изменение ключей. Ещё хочу подвязать базу данных в виде pydantic-обьекта к установщикам более надежным способом (не через симлинки). Вынести обработчики ошибок в отдельный файл и дополнить их. Провести возможный рефакторинг. И как только дошлифую всё это дело, начну делать фронт, и всё к нему подвязывать.

Upd 7: Убраны симлинки и заменены на Redis-хранилище в виде hash-а. В ней хранятся адреса нужных файлов и конфигурации для MongoDB. Передал всё в асинхрон (redis.asyncio, motor вместо pymongo). В итоге имеем контейнер с Redis, который взаимодействует с двумя установщиками. Запускаются все 3 контейнера по отдельности, но для установщиков нужен запущенный redis-config, иначе выдаст закономерную ошибку. Если запустить нужный контейнер, то остальные два работают беспрепятственно, по отдельности и одновременно. Далее нужно сделать настройку Redis-хранилища, потому что пока изменять его можно только через Redis-Commander или терминал. Настройка будет в отдельном контейнере, который будет взаимодействовать только с контейнером вместе с hash-таблицей, как показано на схеме ранее.
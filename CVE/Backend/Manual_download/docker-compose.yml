name: "manual-downloader"

services:
  app:
    build: .
    ports:
      - "8000:8000"
    restart: unless-stopped
    networks:
      - redis-mini-storage-network

networks:
  redis-mini-storage-network:
    external: true

volumes:
  mongodb_data:
